# Project-scoped Codex defaults. Codex applies this file only after the
# repository has been marked as trusted.
approval_policy = "on-request"
sandbox_mode = "workspace-write"
web_search = "live"

[agents]
max_concurrent_threads_per_session = 4
