services:
  hugo:
    build: .
    ports:
      - "1313:1313"
    volumes:
      - .:/src
      - ~/.claude:/root/.claude
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    command: server --bind 0.0.0.0 --buildDrafts --buildFuture
    stdin_open: true
    tty: true
